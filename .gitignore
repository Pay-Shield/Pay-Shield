# Dependencies
node_modules/

# Build output
dist/
.vite/

# Env files (real secrets — keep .env.example tracked)
.env
.env.local
.env.*.local

# Runtime data generated by server.ts (hash-chained + legacy audit logs)
audit_chain.jsonl
legacy_audit_log.jsonl

# Logs
npm-debug.log*
*.log

# Editors / OS
.DS_Store
Thumbs.db

# Claude Code local session/tool state
.claude/

# Old pre-migration snapshot (history preserved via git log; kept on disk for reference only)
project-history/
